{
  "name": "mchverse-slg-contracts",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mud build",
    "test": "mud test",
    "dev": "mud dev-contracts",
    "deploy:local": "mud deploy --profile local --rpc http://127.0.0.1:8545",
    "deploy:testnet": "mud deploy --profile testnet --rpc $RPC_URL_TESTNET",
    "deploy:mainnet": "mud deploy --profile mainnet --rpc $RPC_URL_MAINNET",
    "clean": "rm -rf out cache src/codegen broadcast"
  },
  "dependencies": {
    "@latticexyz/cli": "^2.2.20",
    "@latticexyz/schema-type": "^2.2.20",
    "@latticexyz/store": "^2.2.20",
    "@latticexyz/world": "^2.2.20",
    "@latticexyz/world-modules": "^2.2.20"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
